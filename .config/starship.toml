# https://starship.rs/config/

# Wait 10 milliseconds for starship to check files under the current directory.
scan_timeout = 10

# Disable the newline at the start of the prompt
add_newline = false

format = "$all"

[aws]
symbol = "aws:"

[character]
success_symbol = "[➜](bold green) 😃"
error_symbol = "[✗](bold red) 🤕"

[directory]
truncation_length = 8
truncate_to_repo = false

# we disable slow services
[scala] # scalac is slow
disabled = true

[java] # slow
disabled = true

[battery]
disabled = true

[[battery.display]]
threshold = 20
